import {Secret} from "../src/types/secret";

const secret: Secret = {
    metadata: {
        name: "foo-service",
        namespace: "martin-test",
    },
    data: {
        foo: "SGFsbG8K",
    },
};
